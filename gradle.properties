org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# Loom + IDEA are not configuration-cache clean yet.
org.gradle.configuration-cache=false

# Fabric properties — cross-check at https://fabricmc.net/develop
# 26.2 is unobfuscated, so there is no mappings layer: the names you write are the real ones.
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.17.20
fabric_api_version=0.157.0+26.2

# Bundled (jar-in-jar) libraries
sqlite_version=3.53.0.0

# Mod properties
mod_version=1.0.0
maven_group=dev.lebron
archives_base_name=staffcore

# Test-scope only
junit_version=5.11.4
